# 自建 Komari 主控

## 完整开发分支基线（2026-09-22）

- 后端：完整合入 `komari-monitor/komari` 的 `main`，
  提交 `7d692d2faf276a7c12520faaa8c41c38584bb484`，包含稳定版之后的全部变更。
- 前端：完整合入 `komari-monitor/komari-web` 的 `radix`，
  提交 `aced74240868a1d170f83c8221d0039390c8846e`；正式构建绑定自有完整提交，
  不克隆上游浮动分支，不下载上游预编译前端。
- Agent：覆盖 `komari-monitor/komari-agent` 的 `main@6d68c72eb25d0d71bfaf3a099b101634ceeea5ab`；
  稳定版之后仅有构建剥离符号改动，自建 `1.5.16` 保留这些行为，并按站点维护要求停止写入 Linux MOTD。
- 本次自建主控版本：`1.5.7`。自建版本号与上游版本号分别维护。

本轮明确接受上游的功能删除：剪贴板、终端设置页、访客审计、负载告警及旧内置通知提供者。
通知改用上游的新插件通道接口，内置保留 webhook；以后需要 Telegram 等通道时，
须安装并启用相应通知插件。现有未启用的旧配置保留在数据库备份中，不擅自开启通知。

此前为稳定版部署而记录的上游合并历史不代表代码已同步；本轮使用稳定版作为显式
三方合并基点，将全部开发分支的内容差异真正合入。仅保留自有更新源、安全发布流程、
已修复的运行时问题、测试及旧 Agent 协议兼容层这些独立差异。

保留最新标准版主控的监控、远控、文件管理、SQLite/PostgreSQL、主题和插件功能。
Go module 保留原路径以减少无关代码变动，不因此使用上游主控二进制。
原版权、MIT 许可证和 NOTICE 保留。

## 发布与更新

维护分支为 `owned`；`main` 同步安全构建配置，不自动发布。
稳定和快照发布必须从自有仓库 `owned` 的当前提交手动触发。
工作流固定工具链与 Actions，运行测试并从固定前端提交构建；
全部二进制和 SHA-256 收齐后才公开 Release，镜像复用同轮构建产物。

```text
ghcr.io/r1ddle1337/komari:1.5.7
```

生产部署同时固定镜像摘要。上游代码需人工审查后合入，不自动同步并发布。
面板中 Agent 首装脚本和更新检查均指向自有仓库。
`install-komari.sh` 仅安装本仓库版本，下载后验证配套 SHA-256。

主控没有后台自替换程序；Docker 部署通过维护脚本拉取新镜像。
既有更新脚本必须同时切换到自有 Release API 和 GHCR 仓库。
Agent 仍默认自动检查并安装自有仓库更高版本；Linux 新安装不改写 MOTD，升级只清理完整的旧 Komari 提示块，保留管理员原有内容。

## 数据与兼容性

混合存储部署需同时备份完整数据目录、PostgreSQL 逻辑备份、Compose/环境配置、
更新脚本及原镜像摘要。面板内仅包含 SQLite 的备份不足以恢复全部监控历史。
先拉取验证目标镜像，再停止主控创建一致性备份，保留数据库容器。
升级后核对 API 版本、健康状态、配置和客户端数量、Agent 重连以及插件运行。

上游 1.5 系列调整了旧客户端协议和流量报告通知，迁移时需逐项确认旧节点与插件。
未来涉及数据库结构变化时，失败恢复需要匹配版本的数据库备份，不能只回退镜像。

## 信任边界

自有仓库控制后续版本下发，但不等于审计过全部上游代码或依赖。
GitHub 账号、令牌、协作者、构建依赖和已安装插件/主题仍需保护。
SHA-256 无法防御同时有权替换程序和校验文件的人；这需要另行引入独立签名。
以 root 运行的 Agent 仍允许你配置的面板以 root 远控。

## 1.5.6 诊断与请求改进

堆内存等文本预览超过 1 MiB 时保留受限摘要，返回截断标识，完整二进制下载不受影响。
前端合并同时发起的配置读取，并在保存后失效；离开仪表盘或重新刷新时取消旧历史查询。
回归验证包含预览边界、真实错误、配置读取并发、请求取消，以及页面加载和路由切换。

## 1.5.7 稳定性与资源控制

WebSocket 写入限时 10 秒，关闭连接可中断阻塞写入；RPC 与 Agent 上报限制解压后的消息大小。
历史查询最多同时执行 4 个，单次限时 30 秒，并限制请求点数；近期原始数据超过内存预算时
改用完整聚合结果，不返回被截断的样本。批量 RPC 逐项序列化，避免同时持有多个大型响应。

管理前端固定为 `692d7fe8b0da0e775169a7ef804e5a13b2b0d39f`：保存设置只提交改动字段，
修正 Ping 任务加载状态，合并节点刷新并取消离开页面后的请求。诊断页展示主控、前端与 Go
版本及刷新延迟；通知通道缺少对应插件时提供明确说明。首装 Agent 固定为自有 `1.5.16`。

验证涵盖 Go 全量及相关包竞争检测、超限/取消/聚合完整性测试、前端构建与浏览器故障注入。
